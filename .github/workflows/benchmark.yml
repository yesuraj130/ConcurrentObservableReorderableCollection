name: BenchmarkDotNet CI Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  benchmark:
    name: Run BenchmarkDotNet (.NET 6.0 & .NET 4.8 Matrix)
    runs-on: windows-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '6.0.x'

      - name: Restore Dependencies
        run: dotnet restore dotnet/ConcurrentObservableCollection.sln

      - name: Run Unit Tests (.NET 6.0)
        run: dotnet test dotnet/tests/ConcurrentCollections.Tests/ConcurrentCollections.Tests.csproj --configuration Release --framework net6.0 --verbosity normal

      - name: Run Unit Tests (.NET Framework 4.8)
        run: dotnet test dotnet/tests/ConcurrentCollections.Tests/ConcurrentCollections.Tests.csproj --configuration Release --framework net48 --verbosity normal

      - name: Run BenchmarkDotNet (.NET 6.0)
        run: dotnet run --project dotnet/benchmarks/ConcurrentCollections.Benchmarks/ConcurrentCollections.Benchmarks.csproj -c Release -f net6.0 --filter "*ReorderBenchmarks*" --exporters json

      - name: Run BenchmarkDotNet (.NET Framework 4.8)
        run: dotnet run --project dotnet/benchmarks/ConcurrentCollections.Benchmarks/ConcurrentCollections.Benchmarks.csproj -c Release -f net48 --filter "*ReorderBenchmarks*" --exporters json

      - name: Continuous Benchmark Regression Alert
        uses: benchmark-action/github-action-benchmark@v1
        with:
          tool: 'benchmarkdotnet'
          output-file-path: dotnet/benchmarks/BenchmarkDotNet.Artifacts/results/ConcurrentCollections.Benchmarks.ReorderBenchmarks-report.json
          github-token: ${{ secrets.GITHUB_TOKEN }}
          auto-push: true
          alert-threshold: '130%'
          fail-on-alert: true
          comment-on-alert: true
